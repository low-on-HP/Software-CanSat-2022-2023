const byte AUDIO_BEACON_PIN = 6;

void setup() {
  // put your setup code here, to run once:
  Serial.begin(115200);
}

void loop() {
  // put your main code here, to run repeatedly:
//  analogWriteFrequency(AUDIO_BEACON_PIN, 4000);
//  analogWrite(AUDIO_BEACON_PIN, 10);
//  delay(2000);
//
//  analogWriteFrequency(AUDIO_BEACON_PIN, 0);
//  analogWrite(AUDIO_BEACON_PIN, 0);
//  delay(5000);
}
